<h1> Questions </h1>

<% @questions.each do |question| %>

  <h3># <%= question.id %></h3>
  <h4><%= question.title %></h4>
  <p><%= question.body %></p>
  <p><%= question.created_at %></p>
  <p><%= question.updated_at %></p>

<% end %>